const container = {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#000',
    flexDirection: 'column',
}

const body_container = {
    display: 'flex',
    width: '80%',
    backgroundColor: '#202020',
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'column',
}

const text = {
    color: "#d0d616"
}

const button = {
    width: '40%',
    borderRadius: 20,
    border: 'solid #d0d616',
    backgroundColor: '#202020',
    justifyContent:'center',
    alignItems: 'center',
    color: '#d0d616',
}

const input = {
    width: '70%',
    borderRadius: 20,
    borderColor: '#202020',
    borderBottomColor: '#d0d616',
    marginTop: 10,
    color: "#d0d616",
    borderWidth: 5,
    padding: 10,
}


export{container,body_container,text,button,input}